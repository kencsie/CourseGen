# 不代管使用者機密

## 介紹
使用者的 API key、token 等機密只存在使用者端瀏覽器，server 一律不持久化。

## 為什麼
我們不想當使用者機密的保管者：機密不放 server，外洩的責任就不在我們身上，server 也能維持無狀態、隨時重啟。另一條路是 server 幫使用者保管、換來跨裝置共用的方便；這在別的專案可能合理，但本專案寧可不擔保管的責任。之後新增任何使用者機密，預設一樣只放瀏覽器。

## 相關測試
無。（待補：確認 DB schema 沒有機密欄位、server 端不把機密寫進磁碟。）

## 變更紀錄
無。
